EXPO_PUBLIC_API_URL=http://localhost:8080

# Physical iPhone + Xcode: Metro must be reachable at your Mac's LAN IP (not "localhost" on the phone).
# Uncomment, set your IP (System Settings → Network, or: ipconfig getifaddr en0), same Wi-Fi as the device.
# REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.10
